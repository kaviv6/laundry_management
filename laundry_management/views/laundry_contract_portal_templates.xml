<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_my_home_menu_contract" name="Portal layout : contract menu" inherit_id="portal.portal_breadcrumbs" priority="20">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'contract'" class="breadcrumb-item active">
                Contracts
            </li>
        </xpath>
    </template>

    <template id="portal_my_home_contract" name="Show Contracts" inherit_id="portal.portal_my_home" priority="20">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Contracts</t>
                <t t-set="url" t-value="'/my/contracts'"/>
                <t t-set="placeholder_count" t-value="'contract_count'"/>
            </t>
        </xpath>
    </template>

    <template id="portal_my_contracts" name="My Contracts">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Contracts</t>
            </t>
            <t t-if="not contracts">
                <p>There are currently no contracts for your account.</p>
            </t>
            <t t-if="contracts" t-call="portal.portal_table">
                <thead>
                    <tr class="active">
                        <th>Name</th>
                        <th>Mobile</th>
                        <th>Email</th>
                        <th>Business Type</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    <t t-foreach="contracts" t-as="contract">
                        <tr>
                            <td><span t-field="contract.name"/></td>
                            <td><span t-field="contract.mobile"/></td>
                            <td><span t-field="contract.email"/></td>
                            <td><span t-field="contract.business_type"/></td>
                            <td><span t-field="contract.description"/></td>
                        </tr>
                    </t>
                </tbody>
            </t>
        </t>
    </template>
</odoo>
